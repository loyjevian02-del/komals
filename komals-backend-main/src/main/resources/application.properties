# deploy check: verifying GitHub Actions workflow triggers on this change
spring.application.name=jmaart-catalog-backend
server.port=8083
server.address=127.0.0.1
spring.datasource.url=jdbc:postgresql://localhost:5432/KSpalace
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=komals@123
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Kolkata
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

spring.servlet.multipart.max-file-size=200MB
spring.servlet.multipart.max-request-size=200MB

app.upload.dir=./uploads
app.cors.allowed-origins=http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176,https://jayalakshmihypermart.com,https://admin.jayalakshmihypermart.com

app.jwt.secret=${JWT_SECRET:change-this-dev-secret-change-this-dev-secret}
app.jwt.expiry-hours=${JWT_EXPIRY_HOURS:12}

app.wacrm.webhook-secret=${WACRM_WEBHOOK_SECRET:change-this-dev-secret}

# Server-to-server proxy to wacrm (the WhatsApp CRM) — jmart-admin never
# talks to wacrm directly, only through /admin/crm/** here, so nobody
# needs a separate wacrm login. This is a wacrm API key with the
# `whatsapp_config:manage` scope, not a per-user credential.
app.wacrm.base-url=${WACRM_BASE_URL:https://admin.jayalakshmihypermart.com/crm/api}
app.wacrm.api-key=${WACRM_API_KEY:wacrm_live_ynDnhhp1aWgPevHS1jLVn16Pg6Fuwpe9fyd2y016w2A}

# Set these env vars on first boot to create the admin account; leave blank to skip
app.admin.default-username=kspalace
app.admin.default-password=kspalace@123

